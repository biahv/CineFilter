<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.iff.pooa20162.cinefilter">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PrincipalActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="CineFilter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NovoActivity" />
        <activity android:name=".SenhaActivity" />
        <activity android:name=".FilmeActivity" />
        <activity android:name=".SessaoActivity" />
        <activity android:name=".CinemaActivity" />
        <activity android:name=".PesquisaActivity" />
        <activity android:name=".ConfiguracaoActivity" />
        <activity android:name=".AlterarActivity" />
        <activity android:name=".PesquisadoActivity"></activity>
    </application>

</manifest>